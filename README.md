# CLI Tool Docker

## Опис
Простий CLI інструмент на Python з argparse

## Збірка
docker build -t my-cli-tool:1.0 .

## Запуск

docker run --rm my-cli-tool:1.0

docker run --rm my-cli-tool:1.0 --help

docker run --rm my-cli-tool:1.0 --name Yurii --age 20

# cli-tool-project
